
// LEAGCY SCRIPT - DISABLED FOR BUILD
// This script relied on the old generate-seo-post export which no longer exists.
// TODO: Update this script to use the new Sniper SEO logic if trend detection is needed again.

/*
import OpenAI from 'openai';
import * as cheerio from 'cheerio';
import { generateSEOPost } from './generate-seo-post';

dotenv.config({ path: '.env.local' });

async function main() {
   // ...
}

main();
*/
